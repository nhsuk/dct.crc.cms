parameters:
  releaseEnvironment: ''
  location:           ''

variables:
- name: RESOURCE_GROUP
  value: dct-crccms-rg-${{ parameters.releaseEnvironment }}-${{ parameters.location }}
- name: AZURE_SUBSCRIPTION
  value: dct-crccms-rg-${{ parameters.releaseEnvironment }}
- name: TF_STORAGE_ACCOUNT
  value: crccmstf${{ parameters.releaseEnvironment }}${{ parameters.location }}

# - ${{ if eq(parameters.releaseEnvironment, 'prod') }}:
#   - name: CAMPAIGNS_MONITORING_WEBHOOK
#     value: $(CAMPAIGNS_MONITORING_WEBHOOK_SECRET) ## from variable group in ADO library
# - ${{ else }}:
#   - name: CAMPAIGNS_MONITORING_WEBHOOK
#     value: $(NONPROD_CAMPAIGNS_MONITORING_WEBHOOK) ## from variable group in ADO library